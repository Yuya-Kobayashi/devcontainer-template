# CLAUDE.md

Claude Code reads this file into context every session. The shared source of
truth for Claude Code, Codex, and any other coding agent in this repo is
`AGENTS.md`; it applies in full and is imported below so it loads every session —
including the **always work in a worktree, never edit the main checkout** rule.

@AGENTS.md
